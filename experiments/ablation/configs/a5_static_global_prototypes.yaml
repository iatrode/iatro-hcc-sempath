inherits: tenth_duration_full_population.yaml

# A5: A0 with global L1/L2 reliability prototypes computed once before
# optimization and then fixed. Spatial prototypes continue to track the
# evolving local representation.

runtime:
  output_dir: outputs/experiments/ablation/a5_static_global_prototypes

train:
  dynamic_prototype_refresh_steps: 0
