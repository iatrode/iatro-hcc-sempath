# A8: A0 with L2 gradients deliberately detached from the shared encoder.
# L1 and L2 retain the same activation and weight ramp as A0; only L2 feedback
# into zhcc is removed.

inherits: tenth_duration_full_population.yaml

runtime:
  output_dir: outputs/experiments/ablation/a8_detached_spatial_backbone

loss:
  spatial_detach_shared_encoder: true
