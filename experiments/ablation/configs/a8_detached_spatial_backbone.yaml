# A8: A0 with local spatial-head gradients deliberately detached from the
# shared encoder. L1/L2 schedules, L2 reliability evidence, and spatial-head
# training remain unchanged; only the dense spatial objective cannot update
# z_hcc.

inherits: matched_tenth.yaml

runtime:
  output_dir: outputs/experiments/ablation/a8_detached_spatial_backbone

loss:
  spatial_detach_shared_encoder: true
