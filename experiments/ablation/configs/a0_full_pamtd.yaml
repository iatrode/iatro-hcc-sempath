inherits: ../../../configs/server/train_tenth.example.yaml

# Ablation A0 — dynamic prototypes with half-strength filtering at 1/10 scale.
#
# This historical reference condition is retained specifically for the A0-A5
# dynamic-versus-static prototype contrast. It is not the universal reference
# for the full ablation matrix; A6 is the matched complete-filter condition for
# A6-A2.

runtime:
  output_dir: outputs/experiments/ablation/a0_full_pamtd
