# Shared formal ablation population. The supplied base is the selected Optuna
# A0 trial and remains authoritative for the maximum epoch budget, schedules,
# and joint T/C/S checkpoint-selection rule.

data:
  train_tile_fraction: 0.10
  val_tile_fraction: 0.10
