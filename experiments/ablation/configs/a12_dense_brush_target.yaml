inherits: matched_tenth.yaml

# A12: replace A0's contour-faithful full cell brush/circle range support with
# legacy top-quarter MIL pooling.

runtime:
  output_dir: outputs/experiments/ablation/a12_dense_brush_target

loss:
  spatial_brush_top_fraction: 0.25
