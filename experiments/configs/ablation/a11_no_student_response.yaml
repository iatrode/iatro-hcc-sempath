inherits: matched_tenth.yaml

# Ablation A11 — remove only student prototype-response matching.
#
# Prototype coordinates, direct classification supervision, multi-teacher
# feature/relation distillation, and prototype-adjudicated teacher reliability
# remain identical to A0. This isolates the contribution of the shared
# reliability-weighted semantic response target. The response ramp remains in
# the inherited schedule so checkpoint eligibility is unchanged.

runtime:
  output_dir: outputs/experiments/ablation/a11_no_student_response

loss:
  zhcc_response_weight: 0.0
